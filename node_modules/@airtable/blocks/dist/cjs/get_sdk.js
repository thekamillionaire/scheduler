"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getSdk;
// TODO(alex): prevent sdk sharing across invocations of the same lambda container
var sdk;
/** @hidden */

function getSdk() {
  if (!sdk) {
    var airtableInterface = require('./injected/airtable_interface').default;

    var Sdk = require('./sdk').default;

    sdk = new Sdk(airtableInterface);
  }

  return sdk;
}